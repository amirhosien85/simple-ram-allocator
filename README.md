# Simple RAM Allocator Simulation

این پروژه یک شبیه‌ساز ساده مدیریت حافظه (Memory Management) است که به زبان C نوشته شده.
هدف این پروژه نمایش نحوه کارکرد الگوریتم‌های تخصیص حافظه در سیستم‌عامل‌هاست.

## 🚀 ویژگی‌ها (Features)
- **مدیریت ماژولار:** تفکیک Interface و Implementation.
- **الگوریتم‌های پشتیبانی شده:**
  - `First-Fit`: اولین فضای خالی مناسب را انتخاب می‌کند.
  - `Best-Fit`: فضای خالی‌ای را انتخاب می‌کند که کمترین پرتی را داشته باشد.
- **نمایش گرافیکی:** مشاهده وضعیت بلوک‌های حافظه در کنسول.

## 🛠 نحوه اجرا (How to Run)

### پیش‌نیازها
- کامپایلر GCC
- ابزار Make (اختیاری)

### کامپایل و اجرا
اگر `make` را نصب دارید، فقط کافیست دستور زیر را بزنید:
```bash
make
./simulation
# کامپایل دستی
gcc main.c memory.c -o simulation
./simulation